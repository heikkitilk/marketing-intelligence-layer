# Human-calibrated full-corpus continuation

Date: August 28, 2026 (AMT)

## Scope

[DATA] The completed value-probe review accepted 20 proposals and rejected 2.
This decision ledger calibrates a separate full-corpus run; it does not rewrite
the historical U7 diagnostic receipt.

[DATA] The continuation classifies all 219 dependence groups from the frozen
August 17-24 corpus. It routes every `marketing_bearing` and `mixed_work` group
to extraction, uses the originating harness provider, and retains private,
owner-only inputs and outputs.

[LOGIC] Repetition classes need one model extraction per dependence group, not
one extraction per near-identical session. The representative packet supplies
the group result while the classification and terminal-status ledgers account
for every group.

## Publication boundary

[DATA] Valid extraction output becomes a pending human-review queue. It does
not enter the accepted intelligence layer automatically.

[LOGIC] This preserves the upstream product's useful gate: you decide whether a
proposed learning is worth keeping. Automated faithfulness and candidate
validity checks remain integrity controls for the proposal, not proof that the
proposal is useful.
